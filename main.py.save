import requests


# Compliment API Endpoint:
COMPLIMENT_URL = 'https://complimentr.com/api'
#Two major pieces of this project, Facial Detection and when it detects a face, run the compliment api

def main():
	complimentRequest = requests.get(url=COMPLIMENT_URL)
	complimentData= complimentRequest.json()
	print(complimentData)

def getCompliment():
	complimentRequest = requests.get(url=COMPLIMENT_URL
	return complimentRequest.json()

if __name__ == "__main__":
	main()
